export interface UserItem {
    id: string,
    name: string;
    photoURL: string;
    tags: string[];
    userId: string;
};

export interface UserData {
    username: string;
    tags: string[];
};
